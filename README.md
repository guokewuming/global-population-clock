# 全球人口时钟

纯静态人口模型展示站。页面上的数字基于固定基准与年净变化率在浏览器本地推演，**不代表官方实时统计**。

## 安全边界

- 无后端、数据库、账号或表单
- 无 API Key、环境变量或第三方脚本
- Netlify 仅托管静态文件
- 响应安全头由 `_headers` 设置

## 本地预览

直接打开 `index.html`，或执行 `npx serve .`。
